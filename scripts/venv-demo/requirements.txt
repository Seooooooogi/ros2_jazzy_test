# venv-demo 의존 핀 — containers/{yolo-detection,voice-processing}/Dockerfile 미러 + 모놀리식 추가분.
# 설치 순서/방법은 LAB.md Part A4 참조(numpy<2 는 반드시 마지막). 본 파일은 핀 대조용 reference.
# torch==2.11.0 / torchvision==0.26.0 (cu128) 은 별도 index 라 본문 줄 대신 LAB.md 의 --index-url 명령에 핀(실측: yolo 이미지).
ultralytics<9
opencv-python<4.10
langchain<2
langchain-openai<2
openai<3
pyaudio
sounddevice
scipy<1.18
python-dotenv
pymodbus<4
openwakeword==0.6.0
onnxruntime<2,>=1.10.0
tqdm<5,>=4.0
scikit-learn<2,>=1
requests<3,>=2.0
ai-edge-litert>=2.0.2,<3
numpy<2

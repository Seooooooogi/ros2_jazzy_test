# main 병합 시 main(ours) 버전을 유지할 파일 (한 줄에 하나) — dev 버전으로 덮지 않는다.
# scripts/merge-to-main.sh 가 충돌 시 'git checkout --ours' 로 main 버전을 보존한다.
#
# 제외(.claude-main-exclude)와 다른 의미: 그 파일은 main 에도 "존재"하되 내용은 main 이 직접
# 관리한다. README = main 에서 직접 관리하는 공개 설치 안내(개조식). dev 의 README 는 작업용.
README.md
